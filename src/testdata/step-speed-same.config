[ X-Axis ]
steps-per-mm     = 100
max-feedrate     = 300
max-acceleration = 200
home-pos         = min
range = 500

[ Y-Axis ]
steps-per-mm     = 100
max-feedrate     = 300
max-acceleration = 200
home-pos         = min
range = 500

[ Z-Axis ]
steps-per-mm     = 100
max-feedrate     = 300
max-acceleration = 100
home-pos         = max
range = 100
